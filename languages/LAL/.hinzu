[
  l([
      id(lal),
      name('LAL'),
      expansion('Linguistic Architecture Language'),
      description('A predicate logic-based megamodeling language used for capturing properties of language processors and deriving test cases.'),
      relatesTo(hasPurpose, id(megamodeling))
  ]),
  r([
      id(lal(text)),
      extension(lal),
      extension(lal2),
      relatesTo(representationOf, id(lal))
  ])
].
