[
  l([
      id(esl),
      name('ESL'),
      expansion('Extended Signature Language'),
      headline('A language for abstract syntax definition'),
      details('ESL leverages many-sorted signatures extended with tuples, lists, and primitive types for tree-based abstract syntax.'),
      supersetOf(intern(bsl)),
      facilitates(extern('Syntax definition')),
      facilitates(extern('Object-program representation'))
  ]),
  r([
      id(esl(text)),
      extension(esl),
      representationOf(esl),
      reason(succeed, [
        instanceOf(extern('Algebraic signature')),
	defines(extern('Tree-based abstract syntax'))
      ])
  ])
].
