[
  l([
      id(esl),
      name('ESL'),
      expansion('Extended Signature Language'),
      description('A simple extension of the basic signature language BSL for tree-based abstract syntax definition to support type synonyms, products, lists, maybies, and primitive types.'),
      relatesTo(supersetOf, id(bsl)),
      relatesTo(hasPurpose, id(abssyn)),
      relatesTo(hasPurpose, id(treerepr)),
      relatesTo(hasPurpose, id(langdef))
  ]),
  r([
      id(esl(text)),
      extension(esl),
      relatesTo(representationOf, id(esl))
  ])
].
