[
  l([
      id(bsl),
      name('BSL'),
      expansion('Basic Signature Language'),
      description('A basic specification language for tree-based abstract syntax on the grounds of many-sorted signatures.'),
      relatesTo(subsetOf, id(esl)),
      relatesTo(hasPurpose, id(abssyn)),
      relatesTo(hasPurpose, id(treerepr)),
      relatesTo(hasPurpose, id(langdef))
  ]),
  r([
      id(bsl(text)),
      extension(bsl),
      relatesTo(representationOf, id(bsl))
  ])
].
