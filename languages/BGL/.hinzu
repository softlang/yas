[
  l([
      id(bgl),
      name('BGL'),
      expansion('Basic Grammar Language'),
      description('A BNF-like notation for context-free grammars; rules (productions) are labeled.'),
      relatesTo(similarTo, uri('https://en.wikipedia.org/wiki/Backus%E2%80%93Naur_form')),
      relatesTo(subsetOf, id(egl)),
      relatesTo(hasPurpose, id(textsyn)),
      relatesTo(hasPurpose, id(concsyn)),
      relatesTo(hasPurpose, id(langdef))

  ]),
  r([
      id(bgl(text)),
      extension(bgl),
      relatesTo(representationOf, id(bgl))
  ])
].
