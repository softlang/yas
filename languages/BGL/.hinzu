[
  l([
      id(bgl),
      name('BGL'),
      expansion('Basic Grammar Language'),
      headline('A BNF-like notation for context-free grammars'),
      details('Rules (productions) are labeled.'),
      similarTo(extern('Language:BNF')),
      subsetOf(intern(egl)),
      facilitates(extern('Syntax definition')),
      facilitates(extern('Parsing'))
  ]),
  r([
      id(bgl(text)),
      extension(bgl),
      representationOf(bgl),
      reason(succeed, [
        instanceOf(extern('Context-free grammar')),
	defines(extern('Context-free syntax'))
      ])
  ])
].
