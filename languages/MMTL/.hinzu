[
  l([
      id(mmtl),
      name('MMTL'),
      expansion('MetaModel Transformation Language'),
      headline('A trivial transformation language for metamodels'),
      facilitates(extern('Model transformation')),
      dependsOn(intern(mml))
  ]),
  r([
      id(mmtl(term)),
      representationOf(mmtl)
  ])  
].
