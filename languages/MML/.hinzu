[
  l([
      id(mml),
      name('MML'),
      expansion('MetaModeling Language'),
      headline('A basic metamodeling language'),
      facilitates(extern('Syntax definition')),
      facilitates(extern('Metamodeling'))
  ]),
  r([
      id(mml(text)),
      extension(mml),
      representationOf(mml),
      reason(succeed, [
        instanceOf(extern('Metamodel')),
	defines(extern('Graph-based abstract syntax'))
      ])
  ])
].
