[
  l([
      lid(mml),
      acronym('MML'),
      expansion('MetaModeling Language'),
      explanation('A basic metamodeling language for graph-based abstract syntax.'),
      purpose('Metamodeling'),
      purpose('Abstract syntax definition'),
      purpose('Graph-based abstract syntax definition')
  ]),
  r([
      rid(mml(text)),
      representationOf(mml),
      filenameExtension(mml)
  ])
].
