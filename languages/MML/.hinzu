[
  l([
      id(mml),
      name('MML'),
      expansion('MetaModeling Language'),
      description('A basic metamodeling language for graph-based abstract syntax.'),
      relatesTo(hasPurpose, id(metamodeling)),
      relatesTo(hasPurpose, id(abssyn)),
      relatesTo(hasPurpose, id(graphrepr)),
      relatesTo(hasPurpose, id(langdef))
  ]),
  r([
      id(mml(text)),
      extension(mml),
      relatesTo(representationOf, id(mml))
  ])
].
