begin
  // Sample operands for Euclidian division
  var x = 14;
  var y = 4;

  // Compute quotient q=3 and remainder r=2
  var q = 0;
  var r = x;

  while (r >= y) {
     r = r - y;
     q = q + 1; 
  }
end
