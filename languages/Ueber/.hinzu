[
  l([
      id(ueber),
      name('Ueber'),
      description('A megamodeling language used for build management and regression testing in the YAS repository.'),
      relatesTo(hasPurpose, id(mega)),
      relatesTo(hasPurpose, id(build)),
      relatesTo(hasPurpose, id(regress))
  ]),
  r([
      id(ueber(term)),
      extension(ueber),
      relatesTo(representationOf, id(ueber))
  ])
].
