[
  l([
      id(ueber),
      name('Ueber'),
      headline('A megamodeling language used in the YAS repository'),
      details('Ueber is used for build management and regression testing in the YAS repository.'),
      facilitates(extern('Megamodeling')),
      facilitates(extern('Build management')),
      facilitates(extern('Regression testing'))
  ]),
  r([
      id(ueber(term)),
      extension(ueber),
      representationOf(ueber),
      reason(succeed, [
        instanceOf(extern('Megamodel')),
	supports(extern('Build management')),
        supports(extern('Regression testing'))
      ])
  ])
].
