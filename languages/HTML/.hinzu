[
  l([
      id(html),
      name('HTML'),
      expansion('HyperText Markup Language'),
      headline('The standard markup language HTML for web pages and applications'),
      sameAs(extern('Language:HTML')),
      facilitates(extern('Web publishing'))
  ]),
  r([
      id(html(text)),
      extension(html),
      representationOf(html)
  ])
].
