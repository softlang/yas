[
  l([
      id(html),
      name('HTML'),
      expansion('HyperText Markup Language'),
      description('A standard markup language for web pages and web applications.'),
      relatesTo(sameAs, id('https://en.wikipedia.org/wiki/HTML'))
  ]),
  r([
      id(html(text)),
      extension(html),
      relatesTo(representationOf, id(html))
  ])
].
