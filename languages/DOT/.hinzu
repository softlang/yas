[
  l([
      id(dot),
      name('DOT'),
      headline('The input language of the Graphviz tool'),
      sameAs(extern('Language:DOT')),
      facilitates(extern('Graph visualization')),
      relatesTo(extern('Technology:Graphviz'))
  ]),
  r([
      id(dot(text)),
      extension(dot),
      representationOf(dot),
      reason(succeed, [
        instanceOf(extern('Graph representation'))
      ])
  ])
].

