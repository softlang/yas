[
  l([
      id(asl),
      name('ASL'),
      expansion('Algebraic Specification Language'),
      description('A simple language for algebraic specification.'),
      relatesTo(hasPurpose, id(algspec))
  ]),
  r([
      id(asl(text)),
      extension(asl),
      relatesTo(representationOf, id(asl))
  ])
].
