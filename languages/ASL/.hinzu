[
  l([
      id(asl),
      name('ASL'),
      expansion('Algebraic Specification Language'),
      headline('A simple language for algebraic specification'),
      facilitates(extern('Algebraic specification'))
  ]),
  r([
      id(asl(text)),
      extension(asl),
      representationOf(asl),
      reason(succeed, [
        instanceOf(extern('Algebraic specification'))
      ])
  ])
].
