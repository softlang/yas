[
  l([
      id(dgl),
      name('DGL'),
      expansion('Dot-based Graph Language'),
      description('A trivial specification language for graphs that may be rendered eventually with the Graphviz tool.'),
      relatesTo(hasPurpose, id(graphrepr)),
      relatesTo(subsetOf, id(dot))
  ]),
  r([
      id(dgl(text)),
      extension(dgl),
      relatesTo(representationOf, id(dgl))
  ])
].
