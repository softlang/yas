[
  l([
      id(bstl),
      name('BSTL'),
      expansion('Basic Signature Transformation Language'),
      headline('A transformation language for coupled transformations on signatures and conformant terms'),
      dependsOn(intern(bsl)),
      dependsOn(intern(term))
  ]),
  r([
      id(bstl(term)),
      representationOf(bstl)
  ])
].
