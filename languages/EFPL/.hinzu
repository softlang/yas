[
  l([
      id(efpl),
      name('EFPL'),
      expansion('Extended Functional Programming Language'),
      headline('A trivial first-order functional programming language'),
      details('EFPL is an extension of BFPL to support higher-order functions, polymorphism, and type inference.'),
      relatesTo(extern('Language:Haskell')),
      facilitates(extern('Functional programming')),
      embeds(intern(el)),
      supersetOf(intern(bfpl))
  ]),
  r([
      id(efpl(text)),
      extension(efpl),
      representationOf(efpl),
      reason(succeed, [
        instanceOf(extern('Functional program'))
      ])
  ])
].
