[
  l([
      id(efpl),
      name('EFPL'),
      expansion('Extended Functional Programming Language'),
      description('A small extension of the trivial functional programming language BFPL to provide higher-order functions, polymorphism, and type inference.'),
      relatesTo(supersetOf, id(bfpl)),
      relatesTo(hasPurpose, id(fp))
  ]),
  r([
      id(efpl(text)),
      extension(efpl),
      relatesTo(representationOf, id(efpl))
  ])
].

