[
  l([
      id(ppl),
      name('PPL'),
      expansion('Pretty Printing Language'),
      description('A target language for pretty printing which provides different composition operators for textboxes.')
  ]),
  r([
      id(ppl(term)),
      extension(ppl),
      relatesTo(representationOf, id(ppl))
  ])
].
