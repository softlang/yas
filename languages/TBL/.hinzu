[
  l([
      id(tbl),
      name('TBL'),
      expansion('Tree-based Buddy Language'),
      description('A trivial ontology-oriented language for representing collections of persons and their buddy relationships with a tree-based abstract syntax.'),
      relatesTo(sameAs, id(gbl))
  ]),
  r([
      id(tbl(text)),
      extension(tbl),
      relatesTo(representationOf, id(tbl))
  ])
].
