[
  l([
      id(tbl),
      name('TBL'),
      expansion('Tree-based Buddy Language'),
      headline('A trivial ontology-oriented language'),
      details('TBL is concerned with the representation of collections of persons and their buddy relationships. A textual concrete and a tree-based abstract syntax are leveraged.'),
      sameAs(intern(gbl))
  ]),
  r([
      id(tbl(text)),
      extension(tbl),
      representationOf(tbl)
  ])
].
