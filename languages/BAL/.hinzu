[
  l([
      id(bal),      
      name('BAL'),
      expansion('Basic Assembly Language'),
      headline('A trivial assembly language'),
      details('BAL is stack- and address-based. Memory access and jumps use symbolic names rather than ints.'),
      relatesTo(extern('Language:Java bytecode')),
      facilitates(extern('Compilation')),
      similarTo(intern(bml))
  ])
].
