[
  l([
      id(prolog),
      name('Prolog'),
      headline('A logic programming language'),
      sameAs(extern('Language:Prolog'))
  ]),
  r([
      id(prolog(text)),
      extension(pro),
      processor(load),
      representationOf(prolog)
  ])
].
