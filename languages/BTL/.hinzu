[
  l([
      id(btl),
      name('BTL'),
      expansion('Basic TAPL Language'),
      headline('A trivial expression language'),
      details('There are expression forms on Boolean values and natural numbers inspired by a similar language in Pierce\'s TAPL textbook.'),
      similarTo(intern(el))
  ]),
  r([
      id(btl(text)),
      extension(btl),
      representationOf(btl)
  ])
].
