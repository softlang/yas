[
  l([
      id(tdl),
      name('TDL'),
      expansion('Term Difference Language'),
      headline('A representation format for term differences'),
      facilitates(extern('Model differencing'))
  ]),
  r([
      id(tdl(term)),
      representationOf(tdl)
  ])
].
