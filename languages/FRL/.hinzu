[
  l([
      id(frl),
      name('FRL'),
      expansion('Family by Reference Language'),
      headline('A trivial data model for representing families as collections of persons'),
      details('Persons are identified by reference as opposed to name.'),
      sameAs(intern(fnl))
  ]),
  r([
      id(frl(graph(term))),
      representationOf(frl)
  ])
].
