[
  l([
      id(bml),      
      name('BML'),
      expansion('Basic Machine Language'),
      description('A trivial stack- and address-based machine language; memory access and jumps use ints rather than symbolic names.'),
      relatesTo(hasPurpose, id(compilation)),
      relatesTo(hasPurpose, id(toy)),
      relatesTo(similarTo, id(bal))
  ])
].
