[
  l([
      id(bml),      
      name('BML'),
      expansion('Basic Machine Language'),
      headline('A trivial machine language'),
      details('BML is stack- and address-based. Memory access and jumps use ints rather than symbolic names.'),
      relatesTo(extern('Language:Java bytecode')),
      facilitates(extern('Compilation')),
      similarTo(intern(bal))
  ])
].
