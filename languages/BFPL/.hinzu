[
  l([
      id(bfpl),
      name('BFPL'),
      expansion('Basic Functional Programming Language'),
      headline('A trivial first-order functional programming language'),
      details('BFPL supports primitive types for integer and Boolean values; it does not support algebraic datatypes. BFPL can be viewed as a small fragment of Haskell, but with an eager semantics.'),
      relatesTo(extern('Language:Haskell')),
      facilitates(extern('Functional programming')),
      embeds(intern(el)),
      subsetOf(intern(efpl))
  ]),
  r([
      id(bfpl(text)),
      extension(bfpl),
      representationOf(bfpl),
      reason(succeed, [
        instanceOf(extern('Functional program'))
      ])
  ])
].
