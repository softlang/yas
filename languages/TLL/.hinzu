[
  l([
      id(tll),
      name('TLL'),
      expansion('Typed Lambda Language'),
      description('An applied, typed lambda calculus.'),
      relatesTo(embeds, id(btl)),
      relatesTo(similarTo, id(ull))
  ])
].
