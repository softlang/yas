[
  l([
      id(el),
      name('EL'),
      expansion('Expression Language'),
      headline('A trivial expression language'),
      details('There are arithmetic, relational, and comparison operators on integer and Boolean literals.'),
      similarTo(intern(btl))
  ]),
  r([
      id(el(text)),
      extension(el),
      representationOf(el)
  ])
].
