[
  l([
      id(el),
      name('EL'),
      expansion('Expression Language'),
      description('A trivial expression language with expression forms on integer and Boolean values with arithmetic, relational, and comparison operators.'),
      relatesTo(similarTo, id(btl))
  ]),
  r([
      id(el(text)),
      extension(el),
      relatesTo(representationOf, id(el))
  ])
].
