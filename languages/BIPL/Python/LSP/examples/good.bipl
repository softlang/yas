// Euclidean division: computes quotient q and remainder r of x / y.
// x and y are program inputs (never assigned, read from the initial store).
{
  valid = (x >= 0) && (y > 0);
  q = 0;
  r = x;
  while (r >= y) {
    r = r - y;
    q = q + 1;
  }
  done = r < y;
}
