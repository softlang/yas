[
  l([
      id(hinzu),
      name('Hinzu'),
      headline('An annotation language used in the YAS repository'),
      details('Hinzu serves specifically for the semantic annotation of languages.'),
      facilitates(extern('Semantic annotation'))
  ]),
  r([
      id(hinzu(term)),
      extension(hinzu),
      representationOf(hinzu),
      reason(succeed, [
        instanceOf(extern('Annotation'))
      ])
  ])
].
