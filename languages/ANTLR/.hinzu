[
  l([
      id(antlr),
      name('ANTLR'),
      expansion('Another Tool for Language Recognition'),
      headline('The grammar language of the ANTLR technology'),
      sameAs(extern('Language:ANTLR')),
      relatesTo(extern('Technology:ANTLR')),
      facilitates(extern('Parsing')),
      facilitates(extern('Parser generation'))
  ]),
  r([
      id(antlr(text)),
      extension(g),
      extension(g4),
      representationOf(antlr),
      reason(succeed, [
        instanceOf(extern('Context-free grammar'))
      ]),
      reason(hinzuReasoning:testContextFree, [
        defines(extern('Context-free syntax'))
      ])      
  ])
].
