[
  l([
      id(antlr),
      name('ANTLR'),
      expansion('Another Tool for Language Recognition'),
      description('The ANTLR tool and language for parsing and processing string languages.'),
      relatesTo(sameAs, uri('http://www.antlr.org/')),
      relatesTo(hasPurpose, id(parsing)),
      relatesTo(hasPurpose, id(parsegen))
  ]),
  r([
      id(antlr(text)),
      extension(g),
      extension(g4),
      relatesTo(representationOf, id(antlr))
  ])
].
