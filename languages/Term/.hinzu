[
  l([
      id(term),
      name('Term'),
      headline('Prolog terms as a basic representation format used by the YAS repository')
  ]),
  r([
      id(term),
      representationOf(term)
  ])
].
