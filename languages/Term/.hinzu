[
  l([
      id(term),
      name('Term'),
      description('Prolog terms as a basic representation format used by the YAS repository.')
  ]),
  r([
      id(term),
      relatesTo(representationOf, id(term))
  ])
].
