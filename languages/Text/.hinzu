[
  l([
      id(text),
      name('Text'),
      headline('Text as a basic representation format used by the YAS repository')
  ]),
  r([
      id(text),
      extension(txt),
      representationOf(text)
  ])
].
