[
  l([
      id(text),
      name('Text'),
      description('Text as a basic representation format used by the YAS repository.')
  ]),
  r([
      id(text),
      extension(txt),
      relatesTo(representationOf, id(text))
  ])
].
