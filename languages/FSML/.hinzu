[
  l([
      id(fsml),
      name('FSML'),
      expansion('Finite State Machine Language'),
      description('A simple domain-specific modeling language (DSML) for finite state machines (FSMs).'),
      relatesTo(hasPurpose, id(behavior))
  ]),
  r([
      id(fsml(text)),
      extension(fsml),
      relatesTo(representationOf, id(fsml))
  ])
].
