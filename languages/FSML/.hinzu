[
  l([
      id(fsml),
      name('FSML'),
      expansion('Finite State Machine Language'),
      headline('A domain-specific language for finite state machines'),
      facilitates(extern('Behavioral modeling'))
  ]),
  r([
      id(fsml(text)),
      extension(fsml),
      representationOf(fsml)
  ])
].
