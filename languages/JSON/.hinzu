[
  l([
      id(json),
      name('JSON'),
      expansion('JavaScript Object Notation'),
      description('A dictionary-based interchange format.'),
      relatesTo(sameAs, uri('https://en.wikipedia.org/wiki/JSON')),
      relatesTo(sameAs, uri('http://json.org/')),
      relatesTo(hasPurpose, id(dataexchange)),
      relatesTo(hasPurpose, id(serialization))
  ]),
  r([
      id(json),
      extension(json),
      relatesTo(representationOf, id(json))
  ])
].
