[
  l([
      id(json),
      name('JSON'),
      expansion('JavaScript Object Notation'),
      headline('The well-known JSON format for data exchange'),
      sameAs(extern('Language:JSON')),
      relatesTo(extern('Language:JavaScript')),
      facilitates(extern('Data exchange')),
      facilitates(extern('Serialization'))
  ]),
  r([
      id(json),
      extension(json),
      representationOf(json)
  ])
].
