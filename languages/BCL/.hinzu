[
  l([
      id(bcl),
      name('BCL'),
      expansion('BGL CST Language'),
      description('A representation format for concrete syntax trees (CSTs) for BNF-like grammar notation as of BGL (Basic Grammar Language).'),
      relatesTo(hasPurpose, id(parsetree))
  ]),
  r([
      id(bcl(term)),
      relatesTo(representationOf, id(bcl))
  ])
].
