[
  l([
      id(bcl),
      name('BCL'),
      expansion('BGL CST Language'),
      headline('A format for CSTs for parsing with BGL grammars'),
      dependsOn(intern(bgl)),
      facilitates(extern('Parsing'))
  ]),
  r([
      id(bcl(term)),
      representationOf(bcl)
  ])
].
