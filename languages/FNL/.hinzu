[
  l([
      id(fnl),
      name('FNL'),
      expansion('Family by Name Language'),
      description('A trivial data model for representing families as collections of persons with various attributes; persons are identified by name as opposed to reference.'),
      relatesTo(sameAs, id(frl))
  ]),
  r([
      id(fnl(graph(term))),
      relatesTo(representationOf, id(fnl))
  ])
].
