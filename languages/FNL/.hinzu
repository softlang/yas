[
  l([
      id(fnl),
      name('FNL'),
      expansion('Family by Name Language'),
      headline('A trivial data model for representing families as collections of persons'),
      details('Persons are identified by name as opposed to reference.'),
      sameAs(intern(frl))
  ]),
  r([
      id(fnl(graph(term))),
      representationOf(fnl)
  ])
].
